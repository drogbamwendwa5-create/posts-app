{
  "buildCommand": "cd vite-project && npm run build",
  "outputDirectory": "vite-project/dist",
  "installCommand": "cd vite-project && npm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
